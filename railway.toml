[build]
builder = "NIXPACKS"
buildCommand = "rm -rf dist && pnpm install && pnpm run build:deploy"

[deploy]
startCommand = "pnpm run start:prod"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
